div.order-form-wrapper(ng-controller="OrderFormCtrl")
	div.header-wrapper
		| step 1 เลือกตัวเลือกของคุณ
		//- div.header-triangle
	div.order-step-wrapper.form
		div.main-choice-wrapper
			div.topic#mainOption
				| คุณมีแบบสิ่งพิมพ์ที่ต้องการแล้วหรือยัง?
			div.choice-item
				div.main-choice-item
					div.option-content-wrapper
						label.upload-file-wrapper(ng-click="setMainChoice(0)")#choice1
							label(for="file").upload-file-button
								div.content
									div.main
										| มีแล้ว 
									div.sub
										| คลิ๊กเพื่ออัพโหลดรูปภาพ
							input(type="file", name="file", id="file")		
				div.main-choice-item
					div.option-content-wrapper
						label(ng-click="setMainChoice(1)")#choice2
							div.content
								div.main
									| ให้เราออกแบบให้ 
								div.sub
									| ใช้บริการออกแบบของราชาการพิมพ์ในราคาที่คุ้มค่า!
		div.property-wrapper
			div.property-item
				div.topic#size
					| เลือกขนาด
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showFormat()")#selectedFormat
									img
									div.content
										div.main
											| {{selectedFormat.name}} 
										div.sub
											| {{selectedFormat.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in formats", ng-click="setFormat(list.id, $index)", ng-show="showFormatList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
								li(ng-show="showFormatList", ng-click="setFormat(-1, -1)")
									img
									div.content
										div.main
											| กำหนดขนาดเอง
			div.property-item
				div.topic
					| ขนาดของสิ่งพิมพ์
				div.form
					div.input-wrapper
						input(type="text", disabled, ng-model="formatSize.width", ng-change="setWidth()", placeholder="กว้าง")#width
						span  X 
						input(type="text", disabled, ng-model="formatSize.height", ng-change="setHeight()", placeholder="ยาว")#height
						span  mm
		div.property-wrapper
			div.property-item
				div.topic#size
					| จำนวนหน้า
				div.form
					div.input-wrapper
						input(type="number", min="1", value="1")
			div.property-item
				div.topic#paperType
					| การเข้าเล่ม
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showSelectPaperType()")#selectedPaperType
									img
									div.content
										div.main
											| {{selectedPaperType.name}} 
										div.sub
											| {{selectedPaperType.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in paperTypes | orderBy:'id'", ng-click="setPaperType(list.id, $index)", ng-show="showPaperTypeList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
		div.property-wrapper
			div.property-item
				div.topic#print
					| เลือกประเภทการพิมพ์
				div.selector
					div.control(ng-repeat="list in prints")
						div.option-content-wrapper
							input(type="radio", name="print", id="print{{$index}}", ng-click="setPrint($index)")
							label(for="print{{$index}}")
								img(ng-src="")
								div.content
									div.main
										| {{list.name}}
									div.sub
										| {{list.description}}
			div.property-item
				div.topic#printColor
					| เลือกสีที่ใช้พิมพ์
				div.selector
					div.control(ng-repeat="list in printColors")
						div.option-content-wrapper
							input(type="radio", name="printColor", id="printColor{{$index}}", ng-click="setPrintColor($index)")
							label(for="printColor{{$index}}")
								img(ng-src="")
								div.content
									div.main
										| {{list.name}}
									div.sub
										| {{list.description}}
		div.property-wrapper
			div.property-item
				div.topic#paperType
					| เลือกประเภทกระดาษ
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showSelectPaperType()")#selectedPaperType
									img
									div.content
										div.main
											| {{selectedPaperType.name}} 
										div.sub
											| {{selectedPaperType.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in paperTypes | orderBy:'id'", ng-click="setPaperType(list.id, $index)", ng-show="showPaperTypeList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
			div.property-item
				div.topic#refinement
					| เลือกการเคลือบ
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showRefinement()")#selectedRefinement
									img
									div.content
										div.main
											| {{selectedRefinement.name}} 
										div.sub
											| {{selectedRefinement.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in refinements | orderBy:'id'", ng-click="setRefinement(list.id, $index)", ng-show="showRefinementList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
		div.property-wrapper
			div.property-item
				div.topic#paperType
					| การเจาะรู
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showSelectPaperType()")#selectedPaperType
									img
									div.content
										div.main
											| {{selectedPaperType.name}} 
										div.sub
											| {{selectedPaperType.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in paperTypes | orderBy:'id'", ng-click="setPaperType(list.id, $index)", ng-show="showPaperTypeList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
			div.property-item
				div.topic#refinement
					| การตกแต่ง
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showRefinement()")#selectedRefinement
									img
									div.content
										div.main
											| {{selectedRefinement.name}} 
										div.sub
											| {{selectedRefinement.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in refinements | orderBy:'id'", ng-click="setRefinement(list.id, $index)", ng-show="showRefinementList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
		div.property-wrapper
			div.property-item
				div.topic#paperType
					| รูปทรงสติ๊กเกอร์
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showSelectPaperType()")#selectedPaperType
									img
									div.content
										div.main
											| {{selectedPaperType.name}} 
										div.sub
											| {{selectedPaperType.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in paperTypes | orderBy:'id'", ng-click="setPaperType(list.id, $index)", ng-show="showPaperTypeList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
			div.property-item
				div.topic#refinement
					| Sticker Delivery
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showRefinement()")#selectedRefinement
									img
									div.content
										div.main
											| {{selectedRefinement.name}} 
										div.sub
											| {{selectedRefinement.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in refinements | orderBy:'id'", ng-click="setRefinement(list.id, $index)", ng-show="showRefinementList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
		div.property-wrapper
			div.property-item
				div.topic#paperType
					| การพิมพ์ปก
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showSelectPaperType()")#selectedPaperType
									img
									div.content
										div.main
											| {{selectedPaperType.name}} 
										div.sub
											| {{selectedPaperType.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in paperTypes | orderBy:'id'", ng-click="setPaperType(list.id, $index)", ng-show="showPaperTypeList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
			div.property-item
				div.topic#refinement
					| การพิมพ์ด้านใน
				div.dropdown-wrapper
					ul
						li.dropdown-header
							ul
								li(ng-click="showRefinement()")#selectedRefinement
									img
									div.content
										div.main
											| {{selectedRefinement.name}} 
										div.sub
											| {{selectedRefinement.description}} 
						li.dropdown-selector
							ul
								li(ng-repeat="list in refinements | orderBy:'id'", ng-click="setRefinement(list.id, $index)", ng-show="showRefinementList")
									img
									div.content
										div.main
											| {{list.name}}
										div.sub
											| {{list.description}}
	div.order-step-separator
	div.header-wrapper
		| step 2 เลือกจำนวนและความเร็วในการจัดส่ง
		//- div.header-triangle
	div.order-step-wrapper
		div.order-step-header-wrapper#delivery
			div.order-step-header-row
				div.order-step-header-column
					div.icon
						i.fa.fa-truck
				div.order-step-header-column
					div.sub-order-step-header
						| จัดส่งไปที่
					div.order-step-header
						| กรุงเทพมหานคร
		div.delivery-choice-wrapper
			div.row
				div.column.blank.no-border
					| 
				div.cloumn.table-header.dark-cell.main-table-header
					| ราคาสิ่งพิมพ์พร้อมค่าจัดส่ง
			div.row
				div.column.no-border
					| 
				div.column.soft-table-header
					|	ด่วนพิเศษ
				div.column.soft-table-header
					| วันรับสินค้า
				div.column.soft-table-header
					| วันรับสินค้า
				div.column.soft-table-header
					| วันรับสินค้า
				div.column.soft-table-header
					| วันรับสินค้า
			div.row
				div.column.table-header.dark-cell
					| ปริมาณ
				div.column.delivery-date-wrapper(ng-repeat="dayAndDate in deliveryDayAndDate")
					div.delivery-day
						| {{dayAndDate.day}}
					div.delivery-date
						|	{{dayAndDate.date}}
			div.row(ng-repeat="item in prices")
				div.column.table-header
					| {{item.amount}} ชิ้น
				div.column(ng-if="$index !== 0")
					| 
				div.column.urgent-delivery(ng-if="$index == 0")
					| จัดส่งแบบด่วนพิเศษ?
				div.column.price(ng-repeat="price in item.priceOfDate")
					div.price-wrapper
						input(type="radio", id="price{{$parent.$index}}_{{$index}}", name="price", ng-click="setPrice($parent.$index,$index)")
						label(for="price{{$parent.$index}}_{{$index}}")
							div.full-and-per-one-wrapper
								div.full-price
									| {{price.fullprice}} .-
								div.per-one
									| {{price.perOne}} บาทต่อชิ้น

